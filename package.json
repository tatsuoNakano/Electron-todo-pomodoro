{
  "name": "GlowTime",
  "productName": "GlowTime",
  "version": "1.3.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . PomodoroToDoApp --platform=win32",
    "build": "npx electron-packager . \"GlowTime\" --icon=assets/icon.ico --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^36.5.0",
    "electron-packager": "^17.1.2",
    "electron-reload": "^2.0.0-alpha.1"
  },
  "dependencies": {
    "electron-store": "^10.1.0"
  }
}
